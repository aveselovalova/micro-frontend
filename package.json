{
  "name": "micro-frontend",
  "version": "1.0.0",
  "description": "ModuleFederationPlugin example",
  "author": "aveselovalova",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run --scope @micro-frontend/* --parallel start",
    "build": "lerna run --scope @micro-frontend/* build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
